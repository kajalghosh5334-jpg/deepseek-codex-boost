# Skill: /render-datamodel
# Description: 根据用户具体的业务场景，智能选择【最匹配的 Sample 配色主题】，转化为高颜值、不单调的实体关系图（ERD）和本地交互 Widget。

## 🎨 智能主题调色盘（大模型自主决策路由）
大模型在生成 Mermaid ERD 顶部的 `%%{init: ... }%%` 以及 `data_schema_widget.html` 时，必须根据用户的**业务直觉**，无条件四选一以下最合适的 Sample 视觉皮肤：

1. 🌌 **【极光夜空流】(Cyber Midnight)**
   - **适用场景**：硬核技术、底层架构、高密数据（如外呼台、防冲突、数据库底层、安全隐私）。
   - **核心配色**：背景 `#0F172A`(深蓝黑)，表头边框 `#A855F7`(霓虹紫)，文字 `#94A3B8`。
2. 🥑 **【山系草木流】(Eco Sage)**
   - **适用场景**：健康饮食、营养科学、TikTok 菜谱、生活方式（如 diet2 营养应用）。
   - **核心配色**：背景 `#152219`(森林暗绿)，表头边框 `#10B981`(薄荷绿)，文字 `#A7F3D0`，高亮 `#FBBF24`。
3. 🍊 **【出海淘金流】(Cross-Border Gold)**
   - **适用场景**：跨境电商、dropshipping、供应链、独立站、商业订单变现。
   - **核心配色**：背景 `#1E1B4B`(深夜靛蓝)，表头边框 `#F59E0B`(暖金/琥珀橙)，文字 `#E0E7FF`。
4. 📄 **【白岩极简流】(Alabaster Minimal)**
   - **适用场景**：纯学术讨论、轻量逻辑交代、极简白模。
   - **核心配色**：背景 `#F8FAFC`(纯净白)，表头边框 `#475569`(石墨灰)，文字 `#0F172A`。

## Instructions
1. **第一部分：输出物理实体关系图 (ERD)**
   - 必须使用 ```mermaid 块，且顶部必须无条件注入上述对应主题的 `%%{init: ... }%% erDiagram` 代码头。
   - 严格使用 `erDiagram` 语法，清晰定义表与表之间的级联关系（如 `||--o{`），并且所有主外键（PK/FK）必须在实体内部明确标注，严禁遗漏核心关联键。
   - 实体连线必须清晰，严禁交叉重叠。

2. **第二部分：生成大厂级别的交互式数据字典 (Interactive Widget)**
   - 自动在当前项目目录下创建名为 `data_schema_widget.html` 的独立文件。
   - 这个文件必须是一个极其精美的、带有对应场景感知配色（若为极光夜空流则用深色 `#0F172A` 与天蓝 `#38BDF8`）的前端交互网页。它需要具备以下功能：
     - 左侧：表列表（点击可以切换）；右侧：字段详情表格。
     - 点击字段行可以平滑展开/折叠，展示该字段的【枚举值明细】、【设计决策说明（如为什么要单独拆表）】。
   - 自主使用 Tailwind CSS 进行像素级美化。

3. **第三部分：执行并提示**
   - 写入成功后，自动在后台利用本地能力让浏览器打开它（如 `open data_schema_widget.html`），并回复用户：“*我已为您自主决策并生成了类似 Claude 交互 Widget 的数据结构控制台，已为您自动开屏，您可以直接点击字段展开查看设计决策。*”
EOF
